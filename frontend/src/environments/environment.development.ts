export const environment = {
    // Ajuste a la IP pública/privada donde corre el backend según necesidad.
    // Para usar la IP elástica desde otra máquina o dispositivo, ponga la IP pública:
    API_URL: 'http://54.156.38.225:3000', // Ruta base del backend accesible desde la red
};
